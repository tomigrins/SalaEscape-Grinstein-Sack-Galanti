@{
    ViewData["Title"] = "Video";
}

<body>
    <div id="player"></div>

    @section Scripts {
        <script>
            const videoId = "@ViewBag.video"
            const nextView = "@Url.Action((string)ViewBag.proximaView, "Home")"
            const segundoDeCorte = "@ViewBag.segundoDeCorte"

            let player
            let checkTimeInterval
            function onYouTubeIframeAPIReady() {
                player = new YT.Player('player', {
                    videoId: videoId,
                    playerVars: {
                        autoplay: 1,
                        mute: 1,
                        controls: 0,
                        modestbranding: 1,
                        rel: 0,
                        start: 10
                    },
                    events: {
                        'onReady': function (e) {
                            e.target.playVideo()
                            checkTimeInterval = setInterval(() => {
                                const currentTime = player.getCurrentTime();
                                if (currentTime >= @ViewBag.segundoDeCorte) { 
                                    clearInterval(checkTimeInterval);
                                    window.location.href = "@Url.Action("Mensaje", "Home")";
                                }
                            }, 500)
                        },
                        'onStateChange': function (e) {
                            if (e.data === YT.PlayerState.ENDED) {
                                window.location.href = nextView
                            }
                        }
                    }
                })
            }
        </script>

        <script src="https://www.youtube.com/iframe_api"></script>
    }

</body>
